describe("Conversion Service should pass all tests", () => {
	it.todo("determineConverter should throw an 'NoAvailableConversionWrapperError' because of no wrappers being available")
	it.todo("wrapConversion should throw an error because max-retries value for conversion is reached")
	describe("determineConverter should return the default set highest priority wrapper", () => {
		it.todo("should return unoconv as wrapper")
		it.todo("should return ffmpeg as wrapper")
		it.todo("should return imagemagick as wrapper")
	})
})
// TODO: remove after tests are implemented
export {}